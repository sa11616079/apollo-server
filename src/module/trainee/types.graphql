input CreateUserInput {
    name: String
    email: String
    password: String
    role: String
}

input LoginInput {
    password: String
    email: String
}

input DeleteUserId {
    originalId: String
}

input UpdateUserInput {
    originalId: String
    name: String
    email: String
    role: String
}

input AllTraineesInput {
    skip: Int
    limit: Int
}
